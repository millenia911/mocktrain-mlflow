name: vehicle-detection

docker_env:
  image: millenia911/training-container:pytorch-detectron2

entry_points:
  main: 
    # parameters:
    #   path: {type: string, default:"./"}
    command: "python train.py --path {path}"